import { Wookie } from "./wookie";

/**
 * Class for wookie selfie
 */
export class Selfie {
   
    image?: string ;
    wookie : Wookie;
    
    constructor(){
        this.wookie = new Wookie();
    }
    
    
}
